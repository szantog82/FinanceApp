package com.example.szantog.finance;

/**
 * Created by szantog on 2018.04.04..
 */

public class PopulateData {

     /* for (int i = 0; i < 100; i++) {
            String SALTCHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890";
            StringBuilder salt = new StringBuilder();
            Random rnd = new Random();
            while (salt.length() < 18) { // length of the random string.
                int index = (int) (rnd.nextFloat() * SALTCHARS.length());
                salt.append(SALTCHARS.charAt(index));
            }
            String saltStr = salt.toString();


            repDb.insertData(new RepetitiveItem(System.currentTimeMillis() + (long) (Math.random() * 100), (long) (Math.random() * -1000000), 1423008000000L + (long) (Math.random() * 100000000),
                    0, 1, "", saltStr, String.valueOf(i)));
        }*/

}
